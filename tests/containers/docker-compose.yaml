services:
  homeassistant:
    image: homeassistant/home-assistant:latest
    container_name: homeassistant
    restart: always
    network_mode: bridge 
    volumes:
      - ./tests/config:/config
    ports:
      - 6123:8123
